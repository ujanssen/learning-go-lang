package main

import (
	"fmt"
)


{{range .Xapi}}
{{if eq .Name "session" }}
// {{ .Description }}
type {{ .Name }} struct {
	{{range .Fields}}{{ .Name }}     {{ .Type }}
	{{end}}
}
{{end}}{{end}}

func main() {
		fmt.Println("it compiles")
}
