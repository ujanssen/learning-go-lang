package main

import (
	"fmt"
)

{{range .Xapi}}
// {{ .Description }}
type {{ .Name }} struct {
	{{range .Fields}}
	{{ .Name }}     {{ .Type }}
	{{end}}
}
{{end}}

func main() {
		fmt.Println("it compiles")
}
