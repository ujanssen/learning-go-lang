package main

import (
	"fmt"
)


{{range .Xapi}}
{{if eq .Name "session" }}
{{ $xtype := .Name }}
// {{ .Description }}
type {{ .Name }} struct {
	{{range .Fields}}{{ .Name }}     {{ .Type }}
	{{end}}
}
{{range .Messages}}
{{if eq .Name "login_with_password" }}
func ({{ $xtype }} *{{ $xtype }}) {{ .Name }}(
	{{range .Params}}
		{{ .Name }} {{ .Type }},
	{{end}}
	) {
}
{{end}}{{end}}{{end}}{{end}}

func main() {
		fmt.Println("it compiles")
}
