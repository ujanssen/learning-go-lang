package main

import (
	"fmt"
)


{{range .Xapi}}
{{if eq .Name "session" }}
{{ $xtype := .Name }}
// {{ .Description }}
type {{ .Name }} struct {
	{{range .Fields}}{{ .Name }}     {{ .Type }}
	{{end}}
}
{{range .Messages}}func ({{ $xtype }} *{{ $xtype }}) {{ .Name }}() {
}
{{end}}{{end}}{{end}}

func main() {
		fmt.Println("it compiles")
}
